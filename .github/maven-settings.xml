<?xml version="1.0" encoding="UTF-8"?>
<!--
  CI-only Maven settings.

  Two server entries are needed and actions/setup-java can only write one:
    github                       - the deploy target, matching <distributionManagement>
    github-indiemr-appointments  - the read target, matching the <repositories> id in the root pom

  Both authenticate with the workflow's GITHUB_TOKEN. GitHub Packages requires authentication
  even for public packages, and it resolves org-wide, so the single read entry covers
  indiemroauthprovider-api, billing-api and sms-api as well.

  Local builds are unaffected - developers keep their own ~/.m2/settings.xml.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
  <servers>
    <server>
      <id>github</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
    <server>
      <id>github-indiemr-appointments</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>
</settings>
